# Sandbox Configuration
# Usage: Copy to .env and modify ports for multiple sandboxes
SANDBOX_NAME=app-template

# Public Ports
FRONTEND_PORT=8080
KEYCLOAK_PORT=8180
BACKEND_PORT=9876

# Internal URLs (usually don't need changing)
KEYCLOAK_INTERNAL_URL=http://keycloak:8080

# Realm Configuration
REALM_NAME=app-template
